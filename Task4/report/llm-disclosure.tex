\section{Disclosure of LLM and AI Tool Use}

In many cases, after the manual implementation of for example a classifier was done, LLM-Tooling often suggested an easier way of how it would be implemented using ready made sklearn features. This reduced the error susceptibility because less lines of code were needed, and also sped up the experiments, because many features support multi threading during training (for example the grid search).